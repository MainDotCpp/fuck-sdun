---
alwaysApply: true
---

# Playwright 移动设备反检测模拟项目

## 项目概述

本项目旨在使用 Playwright 创建高度真实的移动设备浏览器环境，模拟 Android 和 iOS 设备，通过精确还原真机参数和特征来规避检测平台的识别。

## 核心目标

1. **真实设备模拟**：模拟 Android 和 iOS 设备的浏览器环境
2. **内核适配**：根据真实设备使用对应的浏览器内核（WebKit for iOS, Chromium/Blink for Android）
3. **反检测能力**：通过精确的设备指纹模拟避免被检测平台识别
4. **参数真实性**：模拟真机的硬件参数、系统参数、浏览器特征
5. **平台特征处理**：正确处理不同平台的限制（如 iOS 无法读取设备内存）

## 技术栈

- **Playwright**: 用于浏览器自动化
- **Node.js/TypeScript**: 推荐使用 TypeScript 以获得更好的类型安全
- **JSON**: 用于存储真机参数库

## 核心功能模块

### 1. 真机参数库 (Device Profiles)
- 存储设备配置信息，包括硬件参数、系统参数、浏览器特征等
- 使用 JSON 格式存储，便于管理和扩展
- 需要包含完整的设备指纹信息（Navigator、WebGL、Canvas、Audio 等）

### 2. 设备管理器 (Device Manager)
- 加载和管理真机参数库
- 根据需求选择合适的设备配置
- 验证设备配置的完整性

### 3. 浏览器上下文配置器 (Browser Context Configurator)
- 根据设备配置创建 Playwright BrowserContext
- 配置视口、用户代理、权限、网络条件等

### 4. 指纹注入器 (Fingerprint Injector)
- 注入 JavaScript 代码修改浏览器环境
- 修改 Navigator、WebGL、Canvas、Audio 等指纹特征
- 处理平台特定限制

### 5. 内核适配器 (Engine Adapter)
- iOS 设备使用 WebKit 内核
- Android 设备使用 Chromium 内核
- 根据设备配置自动选择正确的浏览器类型

## 开发规范

### 平台特征要求

**iOS 设备限制**:
- `navigator.deviceMemory` 必须为 `undefined` 或不存在
- `navigator.plugins` 必须为空数组
- `navigator.mimeTypes` 必须为空数组
- 只能使用 WebKit 内核
- User-Agent 必须符合 iOS Safari 格式

**Android 设备特征**:
- 可以使用 `navigator.deviceMemory`
- 可以使用 Chromium/Blink 内核
- User-Agent 必须符合 Android Chrome 格式
- 支持更多 Web API

### 指纹注入要求

指纹注入需要在页面加载前完成，确保所有指纹修改在页面 JavaScript 执行前生效。需要处理的指纹包括：

1. **基础指纹**：User-Agent、Viewport、屏幕参数、时区
2. **高级指纹**：Navigator 对象属性、WebGL、Canvas、AudioContext、网络信息
3. **行为指纹**（可选）：触摸事件、滚动行为、键盘输入特征

### 代码风格

- **语言**: 使用 TypeScript 编写，确保类型安全
- **类型定义**: 所有设备配置和核心数据结构必须有完整的类型定义
- **错误处理**: 完善的错误处理机制，提供清晰的错误信息
- **代码组织**: 模块化设计，职责清晰，便于维护和扩展
- **日志记录**: 详细的日志记录，便于调试和问题排查
- **命名规范**: 使用清晰、描述性的命名，遵循 TypeScript/JavaScript 命名约定

### 质量要求

- **真实性**: 所有设备参数必须基于真实设备数据，不能随意编造
- **一致性**: 确保所有指纹参数之间的一致性（如屏幕尺寸与视口匹配）
- **平台准确性**: 严格区分 iOS 和 Android 的平台特征和限制
- **内核匹配**: 确保浏览器内核与设备平台匹配
- **可维护性**: 代码结构清晰，便于后续更新和维护

## 测试要求

- 每个设备配置必须通过指纹一致性测试
- 验证不同平台的限制是否正确处理
- 测试反检测能力
- 性能测试，确保注入脚本不影响页面加载速度

## 注意事项

1. **设备参数真实性**: 所有参数必须基于真实设备数据
2. **平台差异**: 严格区分 iOS 和 Android 的平台特征和限制
3. **内核匹配**: 确保浏览器内核与设备平台匹配（iOS=WebKit, Android=Chromium）
4. **指纹一致性**: 确保所有指纹参数之间的一致性
5. **更新维护**: 定期更新设备参数库，添加新设备型号
6. **安全性**: 注意保护设备参数库的访问安全
